# Monitoring Requirements

## Metrics
- Files applied per run
- Failures per run
- Average execution time per file

## Logs
- Endpoint `POST /api/tenants/:id/init-schema` responses
- Audit table entries

## Alerts
- Failure status recorded in audit
- High failure rate over time
